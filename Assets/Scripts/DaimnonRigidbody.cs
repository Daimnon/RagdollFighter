using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum DaimnonForceMode { Force, Acceleration, Impulse, VelocityChange }

public class DaimnonRigidbody : MonoBehaviour
{
    public void AddForce(Vector3 force, DaimnonForceMode mode)
    {
        
    }
}
